import styled from "styled-components";

const PageTitle = styled.h1`
  font-weight: 700;
  color: #222;
  margin-bottom: 0.5rem;
`;

const SectionTitle = styled.h2`
  font-size: 1.75rem;
  font-weight: 600;
  margin: 1rem 0;
  color: #555;
`;

const SubSectionTitle = styled.h3`
  margin-bottom: 2rem;
  color: #555;
`;

export { PageTitle, SectionTitle, SubSectionTitle };
